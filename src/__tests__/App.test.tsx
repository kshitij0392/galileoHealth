import react from 'react';
import { render, fireEvent, waitForElement } from '@testing-library/react';
import { AppProps } from '../components/App';

describe('<App>', () => {
  test('Should display a profile with add provider button', async () => {});
});
